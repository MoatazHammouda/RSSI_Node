# RSSI_Node

```
sudo npm init -y
sudo npm install noble --save
sudo npm install --unsafe-perm bluetooth-hci-socket --save
sudo setcap cap_net_raw+eip $(eval readlink -f `which node`)
sudo node app.js
```
